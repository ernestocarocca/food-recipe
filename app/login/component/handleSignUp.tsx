'use client';


type UserEmailAndPasswordProps = {
children: React.ReactNode;
};

export default function HandleSignUp({ children }: UserEmailAndPasswordProps) {
    return (
        <>{ children } </>
    );
}